Overflow hidden hides the content inside
Overflow scroll makes the content scroll in the container and it gives scroll button

Box sizing: content box gives the parameters only to where the content inside

Icons won't work without the cdn
Font awesome
remove.bg removes the background of an image

position:sticky makes it stick even when there's a scroll
backdrop filter blurs everything behind the container
filter:blur blurs what is inside the container

Letter spacing affects the spaces between letters 
line height affects the space between each lines

clip-path

position:fixed makes it stay in a particular position irrespective of page height 

position: static can't be shifted out of it's original position using top or left etc
It can be moved using transform

position:absolute

to put something in the middle of a container 
you use display flex and justify content


z index deterines the stack order
A negative value brings it behind while a postive value brings it forward 

display:grid breaks your work into row and column
grid template-columns{2,1fr}
think of 2 as the denominator and 1fr as the numerator
row gap give it sapcing between the row
column gap gives it spacing between the column

gap(row column)

column affects the row 

align item moves along the y axis
justify content move along the x axis

place content is the short hand for align content and justify content 

grid column: 1/3;
this is shorthand for grid column start and end 

grid-column: 1 /span 3; meaning start at 1 but span across 3 columns

grid row is the opposite

Animations
There an animation name and animation duration

Keyframes

animation iteration count: infinite; make the animation make it repeat continously

animation direction: reverse; means the order should flow in reverse 

animation fill mode: forward the element will retain the style set by the lastt property


name, duration, timing function, delay, iteration-count, direction
